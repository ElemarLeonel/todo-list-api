{
  "name": "todo-list-api",
  "version": "1.0.0",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "prisma": "prisma",
    "generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.6.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3",
    "prisma": "^5.6.0"
  }
}
